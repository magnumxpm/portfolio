/**
 * Every Focus diagram is padded to this ratio and every Focus well is rendered
 * at it, so the four scenes fill their wells exactly — same height, same width,
 * same bracket frame. Keep this in step with the `aspect-[3/2]` on the well.
 */
export const FOCUS_ASPECT = 3 / 2

/** Project figures sit in a square well. */
export const FIGURE_ASPECT = 1
